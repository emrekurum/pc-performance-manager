<UserControl x:Class="PcPerformanceManager.Views.RamView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:PcPerformanceManager.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" 
                   Text="RAM Management" 
                   FontSize="28" 
                   FontWeight="Bold" 
                   Margin="0,0,0,20"
                   Foreground="#2C3E50"/>

        <!-- RAM Info Card -->
        <Border Grid.Row="1" 
                Background="White" 
                BorderBrush="#BDC3C7" 
                BorderThickness="1" 
                CornerRadius="8" 
                Padding="25" 
                Margin="0,0,0,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0" Margin="0,0,20,0">
                    <TextBlock Text="Total Memory" 
                               FontSize="14" 
                               Foreground="#7F8C8D"
                               Margin="0,0,0,5"/>
                    <TextBlock FontSize="32" FontWeight="Bold" Foreground="#3498DB">
                        <Run Text="{Binding MemoryInfo.TotalGB, Mode=OneWay, StringFormat='{}{0:F2}'}"/>
                        <Run Text=" GB"/>
                    </TextBlock>
                </StackPanel>
                
                <StackPanel Grid.Column="1" Margin="20,0">
                    <TextBlock Text="Used Memory" 
                               FontSize="14" 
                               Foreground="#7F8C8D"
                               Margin="0,0,0,5"/>
                    <TextBlock FontSize="32" FontWeight="Bold" Foreground="#E74C3C">
                        <Run Text="{Binding MemoryInfo.UsedGB, Mode=OneWay, StringFormat='{}{0:F2}'}"/>
                        <Run Text=" GB"/>
                    </TextBlock>
                    <ProgressBar Value="{Binding MemoryInfo.UsedPercentage, Mode=OneWay}" 
                                 Maximum="100" 
                                 Height="10" 
                                 Margin="0,10,0,0"/>
                </StackPanel>
                
                <StackPanel Grid.Column="2" Margin="20,0,0,0">
                    <TextBlock Text="Free Memory" 
                               FontSize="14" 
                               Foreground="#7F8C8D"
                               Margin="0,0,0,5"/>
                    <TextBlock FontSize="24" FontWeight="Bold" Foreground="#27AE60">
                        <Run Text="{Binding MemoryInfo.FreeGB, Mode=OneWay, StringFormat='{}{0:F2}'}"/>
                        <Run Text=" GB"/>
                    </TextBlock>
                    <TextBlock Text="{Binding MemoryInfo.FreePercentage, Mode=OneWay, StringFormat='{}{0:F1}%'}" 
                               FontSize="14" 
                               Foreground="#95A5A6"
                               Margin="0,5,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Controls -->
        <Border Grid.Row="2" 
                Background="White" 
                BorderBrush="#BDC3C7" 
                BorderThickness="1" 
                CornerRadius="8" 
                Padding="20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Toolbar -->
                <Border Grid.Row="0" 
                        Background="#ECF0F1" 
                        Padding="15" 
                        CornerRadius="5" 
                        Margin="0,0,0,15">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <TextBlock Grid.Column="0" 
                                   Text="{Binding StatusMessage}" 
                                   FontSize="14" 
                                   VerticalAlignment="Center"
                                   Foreground="#34495E"/>
                        
                        <CheckBox Grid.Column="1" 
                                  Content="Auto Refresh" 
                                  IsChecked="{Binding IsAutoRefreshEnabled}"
                                  Margin="15,0"
                                  VerticalAlignment="Center"/>
                        
                        <TextBlock Grid.Column="2" 
                                   Text="Interval (sec):" 
                                   VerticalAlignment="Center"
                                   Margin="10,0,5,0"/>
                        <TextBox Grid.Column="3" 
                                 Text="{Binding RefreshIntervalSeconds}" 
                                 Width="50"
                                 VerticalAlignment="Center"/>
                        
                        <StackPanel Grid.Column="3" 
                                    Orientation="Horizontal" 
                                    Margin="60,0,0,0"
                                    HorizontalAlignment="Right">
                            <Button Content="Refresh" 
                                    Command="{Binding RefreshDataCommand}"
                                    IsEnabled="{Binding IsRefreshing, Converter={StaticResource InverseBooleanConverter}}"
                                    Padding="15,8"
                                    Margin="0,0,10,0"
                                    Cursor="Hand"/>
                            <Button Content="Clear All RAM" 
                                    Command="{Binding ClearAllMemoryCommand}"
                                    IsEnabled="{Binding IsClearingMemory, Converter={StaticResource InverseBooleanConverter}}"
                                    Padding="15,8"
                                    Background="#E74C3C"
                                    Foreground="White"
                                    BorderThickness="0"
                                    Cursor="Hand"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Process List -->
                <DataGrid Grid.Row="1" 
                          ItemsSource="{Binding Processes}"
                          SelectedItem="{Binding SelectedProcess}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          GridLinesVisibility="None"
                          HeadersVisibility="Column"
                          SelectionMode="Single"
                          RowBackground="White"
                          AlternatingRowBackground="#F8F9FA">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Process Name" 
                                            Binding="{Binding ProcessName}" 
                                            Width="*"
                                            ElementStyle="{StaticResource DataGridTextStyle}"/>
                        <DataGridTextColumn Header="Memory (MB)" 
                                            Binding="{Binding WorkingSetMB, Mode=OneWay, StringFormat='{}{0:F2}'}" 
                                            Width="150"
                                            ElementStyle="{StaticResource DataGridTextStyle}"/>
                        <DataGridTemplateColumn Header="Actions" Width="150">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Clear" 
                                            Padding="10,5"
                                            Cursor="Hand"
                                            Background="#E74C3C"
                                            Foreground="White"
                                            BorderThickness="0"
                                            Click="ClearProcessButton_Click"
                                            Tag="{Binding ProcessId}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <!-- Loading Indicator -->
        <Border Grid.Row="3" 
                Background="#3498DB" 
                Padding="15" 
                Visibility="{Binding IsRefreshing, Converter={StaticResource BooleanToVisibilityConverter}}"
                Margin="0,10,0,0">
            <TextBlock Text="Refreshing..." 
                       Foreground="White" 
                       FontSize="14"
                       HorizontalAlignment="Center"/>
        </Border>
    </Grid>
</UserControl>
