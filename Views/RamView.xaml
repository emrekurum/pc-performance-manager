<UserControl x:Class="PcPerformanceManager.Views.RamView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:PcPerformanceManager.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="900">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,28">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <StackPanel Grid.Column="0">
                <TextBlock Text="RAM Management" 
                           FontSize="32" 
                           FontWeight="Bold" 
                           Foreground="#F0F6FC"/>
                <TextBlock Text="Monitor and optimize your system memory" 
                           FontSize="14" 
                           Foreground="#8B949E"
                           Margin="0,6,0,0"/>
            </StackPanel>
            
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="Clear All RAM" 
                        Command="{Binding ClearAllMemoryCommand}"
                        IsEnabled="{Binding IsClearingMemory, Converter={StaticResource InverseBooleanConverter}}"
                        Style="{StaticResource DangerButton}"
                        Margin="0,0,12,0"/>
                <Button Command="{Binding RefreshDataCommand}"
                        IsEnabled="{Binding IsRefreshing, Converter={StaticResource InverseBooleanConverter}}"
                        Style="{StaticResource PrimaryButton}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="🔄" FontSize="14" Margin="0,0,8,0"/>
                        <TextBlock Text="Refresh"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>

        <!-- RAM Info Cards -->
        <UniformGrid Grid.Row="1" Columns="3" Margin="0,0,0,24">
            <!-- Total Memory Card -->
            <Border Margin="0,0,10,0" CornerRadius="14" Padding="24"
                    Background="#161B22" BorderThickness="1" BorderBrush="#30363D">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                        <Border Width="44" Height="44" CornerRadius="12" Margin="0,0,14,0">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="#2058A6FF" Offset="0"/>
                                    <GradientStop Color="#1058A6FF" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <TextBlock Text="📊" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Total Memory" FontSize="13" Foreground="#8B949E"/>
                        </StackPanel>
                    </StackPanel>
                    <TextBlock Text="{Binding TotalMemoryDisplay, Mode=OneWay}" 
                               FontSize="36" 
                               FontWeight="Bold" 
                               Foreground="#58A6FF"/>
                </StackPanel>
            </Border>
            
            <!-- Used Memory Card -->
            <Border Margin="5,0" CornerRadius="14" Padding="24"
                    Background="#161B22" BorderThickness="1" BorderBrush="#30363D">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                        <Border Width="44" Height="44" CornerRadius="12" Margin="0,0,14,0">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="#20F78166" Offset="0"/>
                                    <GradientStop Color="#10F78166" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <TextBlock Text="🔥" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Used Memory" FontSize="13" Foreground="#8B949E"/>
                            <TextBlock Text="{Binding MemoryInfo.UsedPercentage, Mode=OneWay, StringFormat='{}{0:F1}%'}" 
                                       FontSize="11" Foreground="#F78166" Margin="0,2,0,0"/>
                        </StackPanel>
                    </StackPanel>
                    <TextBlock Text="{Binding UsedMemoryDisplay, Mode=OneWay}" 
                               FontSize="36" 
                               FontWeight="Bold" 
                               Foreground="#F78166"/>
                    <ProgressBar Value="{Binding MemoryInfo.UsedPercentage, Mode=OneWay}" 
                                 Maximum="100" 
                                 Height="6"
                                 Style="{StaticResource ProgressBarOrange}"
                                 Margin="0,12,0,0"/>
                </StackPanel>
            </Border>
            
            <!-- Free Memory Card -->
            <Border Margin="10,0,0,0" CornerRadius="14" Padding="24"
                    Background="#161B22" BorderThickness="1" BorderBrush="#30363D">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                        <Border Width="44" Height="44" CornerRadius="12" Margin="0,0,14,0">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="#203FB950" Offset="0"/>
                                    <GradientStop Color="#103FB950" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <TextBlock Text="✨" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Free Memory" FontSize="13" Foreground="#8B949E"/>
                            <TextBlock Text="{Binding MemoryInfo.FreePercentage, Mode=OneWay, StringFormat='{}{0:F1}%'}" 
                                       FontSize="11" Foreground="#3FB950" Margin="0,2,0,0"/>
                        </StackPanel>
                    </StackPanel>
                    <TextBlock Text="{Binding FreeMemoryDisplay, Mode=OneWay}" 
                               FontSize="36" 
                               FontWeight="Bold" 
                               Foreground="#3FB950"/>
                </StackPanel>
            </Border>
        </UniformGrid>

        <!-- Process List Section -->
        <Border Grid.Row="2" 
                Background="#161B22" 
                BorderBrush="#30363D" 
                BorderThickness="1" 
                CornerRadius="14" 
                Padding="0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Toolbar -->
                <Border Grid.Row="0" 
                        Background="#21262D" 
                        Padding="20,16"
                        CornerRadius="14,14,0,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <StackPanel Grid.Column="0" Orientation="Horizontal">
                            <TextBlock Text="Running Processes" 
                                       FontSize="16" 
                                       FontWeight="SemiBold"
                                       Foreground="#F0F6FC"
                                       VerticalAlignment="Center"/>
                            <Border Background="#30363D" 
                                    CornerRadius="10" 
                                    Padding="10,4"
                                    Margin="12,0,0,0"
                                    VerticalAlignment="Center">
                                <TextBlock Text="{Binding StatusMessage}" 
                                           FontSize="12" 
                                           Foreground="#8B949E"/>
                            </Border>
                        </StackPanel>
                        
                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                            <CheckBox Content="Auto Refresh" 
                                      IsChecked="{Binding IsAutoRefreshEnabled}"
                                      Foreground="#8B949E"
                                      VerticalAlignment="Center"
                                      Style="{StaticResource ModernCheckBox}"/>
                            
                            <TextBlock Text="Interval:" 
                                       VerticalAlignment="Center"
                                       Foreground="#8B949E"
                                       Margin="20,0,8,0"
                                       FontSize="13"/>
                            <TextBox Text="{Binding RefreshIntervalSeconds}" 
                                     Width="50"
                                     Style="{StaticResource ModernTextBox}"
                                     Padding="8,6"
                                     VerticalAlignment="Center"/>
                            <TextBlock Text="sec" 
                                       VerticalAlignment="Center"
                                       Foreground="#484F58"
                                       Margin="6,0,0,0"
                                       FontSize="12"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Process DataGrid -->
                <DataGrid Grid.Row="1" 
                          ItemsSource="{Binding Processes}"
                          SelectedItem="{Binding SelectedProcess}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          GridLinesVisibility="None"
                          HeadersVisibility="Column"
                          SelectionMode="Single"
                          Background="Transparent"
                          Foreground="#F0F6FC"
                          BorderThickness="0"
                          RowBackground="#161B22"
                          AlternatingRowBackground="#1C2128">
                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#21262D"/>
                            <Setter Property="Foreground" Value="#8B949E"/>
                            <Setter Property="Padding" Value="16,12"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="FontSize" Value="13"/>
                            <Setter Property="BorderThickness" Value="0,0,0,1"/>
                            <Setter Property="BorderBrush" Value="#30363D"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>
                    <DataGrid.RowStyle>
                        <Style TargetType="DataGridRow">
                            <Setter Property="Background" Value="#161B22"/>
                            <Setter Property="Foreground" Value="#F0F6FC"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#21262D"/>
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="#1A58A6FF"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.RowStyle>
                    <DataGrid.CellStyle>
                        <Style TargetType="DataGridCell">
                            <Setter Property="Foreground" Value="#F0F6FC"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Padding" Value="8"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="Foreground" Value="#F0F6FC"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.CellStyle>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Process Name" 
                                            Binding="{Binding ProcessName}" 
                                            Width="*">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Padding" Value="16,10"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="Foreground" Value="#F0F6FC"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="Memory (MB)" 
                                            Binding="{Binding WorkingSetMB, Mode=OneWay, StringFormat='{}{0:F2}'}" 
                                            Width="150">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Padding" Value="16,10"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="Foreground" Value="#D29922"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTemplateColumn Header="Actions" Width="120">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Clear" 
                                            Padding="14,6"
                                            Cursor="Hand"
                                            Click="ClearProcessButton_Click"
                                            Tag="{Binding ProcessId}">
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Setter Property="Background">
                                                    <Setter.Value>
                                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                            <GradientStop Color="#F78166" Offset="0"/>
                                                            <GradientStop Color="#DA3633" Offset="1"/>
                                                        </LinearGradientBrush>
                                                    </Setter.Value>
                                                </Setter>
                                                <Setter Property="Foreground" Value="White"/>
                                                <Setter Property="BorderThickness" Value="0"/>
                                                <Setter Property="FontSize" Value="12"/>
                                                <Setter Property="FontWeight" Value="SemiBold"/>
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="Button">
                                                            <Border x:Name="border" 
                                                                    Background="{TemplateBinding Background}" 
                                                                    CornerRadius="6"
                                                                    Padding="{TemplateBinding Padding}">
                                                                <ContentPresenter HorizontalAlignment="Center" 
                                                                                  VerticalAlignment="Center"/>
                                                            </Border>
                                                            <ControlTemplate.Triggers>
                                                                <Trigger Property="IsMouseOver" Value="True">
                                                                    <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                                                                </Trigger>
                                                            </ControlTemplate.Triggers>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </Style>
                                        </Button.Style>
                                    </Button>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <!-- Loading Indicator -->
        <Border Grid.Row="3" 
                Padding="16" 
                CornerRadius="10"
                Visibility="{Binding IsRefreshing, Converter={StaticResource BooleanToVisibilityConverter}}"
                Margin="0,16,0,0">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                    <GradientStop Color="#58A6FF" Offset="0"/>
                    <GradientStop Color="#388BFD" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBlock Text="⏳" FontSize="14" Margin="0,0,10,0"/>
                <TextBlock Text="Refreshing process list..." 
                           Foreground="White" 
                           FontSize="14"
                           FontWeight="SemiBold"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
