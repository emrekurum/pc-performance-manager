<UserControl x:Class="PcPerformanceManager.Views.RamView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:PcPerformanceManager.Converters"
             xmlns:models="clr-namespace:PcPerformanceManager.Models"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="900">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <StackPanel Grid.Column="0">
                <TextBlock Text="RAM Management" 
                           FontSize="32" 
                           FontWeight="Bold" 
                           Foreground="#F0F6FC"/>
                <TextBlock Text="Monitor and optimize your system memory" 
                           FontSize="14" 
                           Foreground="#8B949E"
                           Margin="0,6,0,0"/>
            </StackPanel>
            
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Command="{Binding RefreshDataCommand}"
                        IsEnabled="{Binding IsRefreshing, Converter={StaticResource InverseBooleanConverter}}"
                        Style="{StaticResource PrimaryButton}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="🔄" FontSize="14" Margin="0,0,8,0"/>
                        <TextBlock Text="Yenile"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>

        <!-- RAM Info Cards -->
        <UniformGrid Grid.Row="1" Columns="3" Margin="0,0,0,20">
            <!-- Total Memory Card -->
            <Border Margin="0,0,10,0" CornerRadius="14" Padding="20"
                    Background="#161B22" BorderThickness="1" BorderBrush="#30363D">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                        <Border Width="40" Height="40" CornerRadius="10" Margin="0,0,12,0">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="#2058A6FF" Offset="0"/>
                                    <GradientStop Color="#1058A6FF" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <TextBlock Text="📊" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <TextBlock Text="Toplam RAM" FontSize="13" Foreground="#8B949E" VerticalAlignment="Center"/>
                    </StackPanel>
                    <TextBlock Text="{Binding TotalMemoryDisplay, Mode=OneWay}" 
                               FontSize="32" FontWeight="Bold" Foreground="#58A6FF"/>
                </StackPanel>
            </Border>
            
            <!-- Used Memory Card -->
            <Border Margin="5,0" CornerRadius="14" Padding="20"
                    Background="#161B22" BorderThickness="1" BorderBrush="#30363D">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                        <Border Width="40" Height="40" CornerRadius="10" Margin="0,0,12,0">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="#20F78166" Offset="0"/>
                                    <GradientStop Color="#10F78166" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <TextBlock Text="🔥" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Kullanılan" FontSize="13" Foreground="#8B949E"/>
                            <TextBlock Text="{Binding MemoryInfo.UsedPercentage, Mode=OneWay, StringFormat='{}{0:F1}%'}" 
                                       FontSize="11" Foreground="#F78166"/>
                        </StackPanel>
                    </StackPanel>
                    <TextBlock Text="{Binding UsedMemoryDisplay, Mode=OneWay}" 
                               FontSize="32" FontWeight="Bold" Foreground="#F78166"/>
                    <ProgressBar Value="{Binding MemoryInfo.UsedPercentage, Mode=OneWay}" Maximum="100" 
                                 Height="6" Style="{StaticResource ProgressBarOrange}" Margin="0,10,0,0"/>
                </StackPanel>
            </Border>
            
            <!-- Free Memory Card -->
            <Border Margin="10,0,0,0" CornerRadius="14" Padding="20"
                    Background="#161B22" BorderThickness="1" BorderBrush="#30363D">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                        <Border Width="40" Height="40" CornerRadius="10" Margin="0,0,12,0">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="#203FB950" Offset="0"/>
                                    <GradientStop Color="#103FB950" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <TextBlock Text="✨" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Boş" FontSize="13" Foreground="#8B949E"/>
                            <TextBlock Text="{Binding MemoryInfo.FreePercentage, Mode=OneWay, StringFormat='{}{0:F1}%'}" 
                                       FontSize="11" Foreground="#3FB950"/>
                        </StackPanel>
                    </StackPanel>
                    <TextBlock Text="{Binding FreeMemoryDisplay, Mode=OneWay}" 
                               FontSize="32" FontWeight="Bold" Foreground="#3FB950"/>
                </StackPanel>
            </Border>
        </UniformGrid>

        <!-- Main Content - TabControl -->
        <TabControl Grid.Row="2" Background="Transparent" BorderThickness="0">
            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="#8B949E"/>
                    <Setter Property="Padding" Value="20,12"/>
                    <Setter Property="FontSize" Value="14"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TabItem">
                                <Border x:Name="border" Background="Transparent" Padding="{TemplateBinding Padding}" 
                                        CornerRadius="10,10,0,0" Margin="0,0,4,0">
                                    <ContentPresenter ContentSource="Header" HorizontalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="border" Property="Background" Value="#161B22"/>
                                        <Setter Property="Foreground" Value="#F0F6FC"/>
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="border" Property="Background" Value="#21262D"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TabControl.Resources>

            <!-- Tab 1: Bloatware Analizi -->
            <TabItem Header="🧹 Gereksiz Uygulamalar">
                <Border Background="#161B22" CornerRadius="0,14,14,14" BorderThickness="1" BorderBrush="#30363D">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!-- Toolbar -->
                        <Border Grid.Row="0" Background="#21262D" Padding="20,16">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" Orientation="Horizontal">
                                    <Button Command="{Binding AnalyzeBloatwareCommand}"
                                            IsEnabled="{Binding IsAnalyzing, Converter={StaticResource InverseBooleanConverter}}"
                                            Style="{StaticResource PrimaryButton}" Padding="16,10" Margin="0,0,10,0">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="🔍" FontSize="14" Margin="0,0,8,0"/>
                                            <TextBlock Text="Analiz Et"/>
                                        </StackPanel>
                                    </Button>
                                    <Button Command="{Binding AutoCleanBloatwareCommand}"
                                            IsEnabled="{Binding IsClearingMemory, Converter={StaticResource InverseBooleanConverter}}"
                                            Style="{StaticResource DangerButton}" Padding="16,10">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="🗑️" FontSize="14" Margin="0,0,8,0"/>
                                            <TextBlock Text="Otomatik Temizle"/>
                                        </StackPanel>
                                    </Button>
                                </StackPanel>

                                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <Border Background="#30363D" CornerRadius="8" Padding="12,6" Margin="0,0,10,0"
                                            Visibility="{Binding BloatwareCount, Converter={StaticResource CountToVisibilityConverter}}">
                                        <TextBlock Foreground="#F78166" FontSize="13">
                                            <Run Text="{Binding BloatwareCount}" FontWeight="Bold"/>
                                            <Run Text=" uygulama bulundu"/>
                                        </TextBlock>
                                    </Border>
                                    <Border Background="#30363D" CornerRadius="8" Padding="12,6"
                                            Visibility="{Binding BloatwareCount, Converter={StaticResource CountToVisibilityConverter}}">
                                        <TextBlock Foreground="#D29922" FontSize="13">
                                            <Run Text="{Binding BloatwareTotalMB, StringFormat='{}{0:F1}'}" FontWeight="Bold"/>
                                            <Run Text=" MB kullanılıyor"/>
                                        </TextBlock>
                                    </Border>
                                </StackPanel>

                                <StackPanel Grid.Column="2" Orientation="Horizontal">
                                    <Button Content="Tümünü Seç" Command="{Binding SelectAllBloatwareCommand}"
                                            Style="{StaticResource SecondaryButton}" Padding="12,8" FontSize="12" Margin="0,0,8,0"/>
                                    <Button Content="Güvenli Seç" Command="{Binding SelectSafeBloatwareCommand}"
                                            Style="{StaticResource SecondaryButton}" Padding="12,8" FontSize="12" Margin="0,0,8,0"/>
                                    <Button Command="{Binding CleanBloatwareCommand}"
                                            IsEnabled="{Binding IsClearingMemory, Converter={StaticResource InverseBooleanConverter}}"
                                            Style="{StaticResource SuccessButton}" Padding="16,10">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="✓" FontSize="14" Margin="0,0,8,0"/>
                                            <TextBlock Text="Seçilenleri Kapat"/>
                                        </StackPanel>
                                    </Button>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <!-- Info Banner -->
                        <Border Grid.Row="1" Margin="20,16,20,0" Padding="16" CornerRadius="10"
                                Visibility="{Binding BloatwareCount, Converter={StaticResource CountToVisibilityConverter}, ConverterParameter=inverse}">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                    <GradientStop Color="#1558A6FF" Offset="0"/>
                                    <GradientStop Color="#0A58A6FF" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="💡" FontSize="18" Margin="0,0,12,0"/>
                                <TextBlock Text="Gereksiz uygulamaları bulmak için 'Analiz Et' butonuna tıklayın. Bulunan uygulamalar güvenle kapatılabilir." 
                                           Foreground="#58A6FF" FontSize="13" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>

                        <!-- Bloatware List -->
                        <DataGrid Grid.Row="2" ItemsSource="{Binding BloatwareProcesses}"
                                  AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False"
                                  GridLinesVisibility="None" HeadersVisibility="Column" SelectionMode="Single"
                                  Background="Transparent" Foreground="#F0F6FC" BorderThickness="0"
                                  RowBackground="#161B22" AlternatingRowBackground="#1C2128" Margin="0,10,0,0">
                            <DataGrid.ColumnHeaderStyle>
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="Background" Value="#21262D"/>
                                    <Setter Property="Foreground" Value="#8B949E"/>
                                    <Setter Property="Padding" Value="16,12"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                    <Setter Property="FontSize" Value="13"/>
                                    <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                    <Setter Property="BorderBrush" Value="#30363D"/>
                                </Style>
                            </DataGrid.ColumnHeaderStyle>
                            <DataGrid.RowStyle>
                                <Style TargetType="DataGridRow">
                                    <Setter Property="Background" Value="#161B22"/>
                                    <Setter Property="BorderThickness" Value="0"/>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#21262D"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGrid.RowStyle>
                            <DataGrid.Columns>
                                <DataGridCheckBoxColumn Header="" Binding="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}" Width="50">
                                    <DataGridCheckBoxColumn.ElementStyle>
                                        <Style TargetType="CheckBox">
                                            <Setter Property="HorizontalAlignment" Value="Center"/>
                                        </Style>
                                    </DataGridCheckBoxColumn.ElementStyle>
                                </DataGridCheckBoxColumn>
                                <DataGridTextColumn Header="Uygulama" Binding="{Binding DisplayName}" Width="180">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Padding" Value="12,8"/>
                                            <Setter Property="Foreground" Value="#F0F6FC"/>
                                            <Setter Property="FontWeight" Value="SemiBold"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTemplateColumn Header="Kategori" Width="140">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Border Background="#21262D" CornerRadius="6" Padding="8,4" Margin="4" HorizontalAlignment="Left">
                                                <TextBlock Text="{Binding Category}" FontSize="11" Foreground="#A371F7" FontWeight="SemiBold"/>
                                            </Border>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTextColumn Header="Açıklama" Binding="{Binding Description}" Width="*">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Padding" Value="12,8"/>
                                            <Setter Property="Foreground" Value="#8B949E"/>
                                            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="RAM (MB)" Binding="{Binding MemoryMB, StringFormat='{}{0:F1}'}" Width="100">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Padding" Value="12,8"/>
                                            <Setter Property="Foreground" Value="#D29922"/>
                                            <Setter Property="FontWeight" Value="Bold"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTemplateColumn Header="Risk" Width="80">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Border CornerRadius="6" Padding="8,4" Margin="4" HorizontalAlignment="Center">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Setter Property="Background" Value="#203FB950"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding RiskLevel}" Value="{x:Static models:ProcessRiskLevel.Low}">
                                                                <Setter Property="Background" Value="#20D29922"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding RiskLevel}" Value="{x:Static models:ProcessRiskLevel.Medium}">
                                                                <Setter Property="Background" Value="#20F78166"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>
                                                <TextBlock FontSize="10" FontWeight="Bold" HorizontalAlignment="Center">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Text" Value="Güvenli"/>
                                                            <Setter Property="Foreground" Value="#3FB950"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding RiskLevel}" Value="{x:Static models:ProcessRiskLevel.Low}">
                                                                    <Setter Property="Text" Value="Düşük"/>
                                                                    <Setter Property="Foreground" Value="#D29922"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding RiskLevel}" Value="{x:Static models:ProcessRiskLevel.Medium}">
                                                                    <Setter Property="Text" Value="Orta"/>
                                                                    <Setter Property="Foreground" Value="#F78166"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>
                                            </Border>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </Border>
            </TabItem>

            <!-- Tab 2: Tüm Processler -->
            <TabItem Header="📋 Tüm Processler">
                <Border Background="#161B22" CornerRadius="0,14,14,14" BorderThickness="1" BorderBrush="#30363D">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!-- Toolbar -->
                        <Border Grid.Row="0" Background="#21262D" Padding="20,16">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                
                                <StackPanel Grid.Column="0" Orientation="Horizontal">
                                    <TextBlock Text="Çalışan Processler" FontSize="16" FontWeight="SemiBold" Foreground="#F0F6FC" VerticalAlignment="Center"/>
                                    <Border Background="#30363D" CornerRadius="10" Padding="10,4" Margin="12,0,0,0" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding StatusMessage}" FontSize="12" Foreground="#8B949E"/>
                                    </Border>
                                </StackPanel>
                                
                                <StackPanel Grid.Column="1" Orientation="Horizontal">
                                    <Button Content="RAM Temizle" Command="{Binding ClearAllMemoryCommand}"
                                            IsEnabled="{Binding IsClearingMemory, Converter={StaticResource InverseBooleanConverter}}"
                                            Style="{StaticResource DangerButton}" Padding="16,10" Margin="0,0,10,0"/>
                                    <CheckBox Content="Otomatik Yenile" IsChecked="{Binding IsAutoRefreshEnabled}"
                                              Foreground="#8B949E" VerticalAlignment="Center" Style="{StaticResource ModernCheckBox}" Margin="0,0,15,0"/>
                                    <TextBlock Text="Aralık:" VerticalAlignment="Center" Foreground="#8B949E" Margin="0,0,8,0" FontSize="13"/>
                                    <TextBox Text="{Binding RefreshIntervalSeconds}" Width="50" Style="{StaticResource ModernTextBox}" Padding="8,6" VerticalAlignment="Center"/>
                                    <TextBlock Text="sn" VerticalAlignment="Center" Foreground="#484F58" Margin="6,0,0,0" FontSize="12"/>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <!-- Process DataGrid -->
                        <DataGrid Grid.Row="1" ItemsSource="{Binding Processes}" SelectedItem="{Binding SelectedProcess}"
                                  AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False"
                                  GridLinesVisibility="None" HeadersVisibility="Column" SelectionMode="Single"
                                  Background="Transparent" Foreground="#F0F6FC" BorderThickness="0"
                                  RowBackground="#161B22" AlternatingRowBackground="#1C2128">
                            <DataGrid.ColumnHeaderStyle>
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="Background" Value="#21262D"/>
                                    <Setter Property="Foreground" Value="#8B949E"/>
                                    <Setter Property="Padding" Value="16,12"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                    <Setter Property="FontSize" Value="13"/>
                                    <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                    <Setter Property="BorderBrush" Value="#30363D"/>
                                </Style>
                            </DataGrid.ColumnHeaderStyle>
                            <DataGrid.RowStyle>
                                <Style TargetType="DataGridRow">
                                    <Setter Property="Background" Value="#161B22"/>
                                    <Setter Property="BorderThickness" Value="0"/>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#21262D"/>
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter Property="Background" Value="#1A58A6FF"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGrid.RowStyle>
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Process Adı" Binding="{Binding ProcessName}" Width="*">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Padding" Value="16,10"/>
                                            <Setter Property="Foreground" Value="#F0F6FC"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="Bellek (MB)" Binding="{Binding WorkingSetMB, StringFormat='{}{0:F2}'}" Width="150">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Padding" Value="16,10"/>
                                            <Setter Property="Foreground" Value="#D29922"/>
                                            <Setter Property="FontWeight" Value="SemiBold"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTemplateColumn Header="İşlem" Width="120">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Content="Temizle" Padding="14,6" Cursor="Hand" Click="ClearProcessButton_Click" Tag="{Binding ProcessId}">
                                                <Button.Style>
                                                    <Style TargetType="Button">
                                                        <Setter Property="Background">
                                                            <Setter.Value>
                                                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                                    <GradientStop Color="#F78166" Offset="0"/>
                                                                    <GradientStop Color="#DA3633" Offset="1"/>
                                                                </LinearGradientBrush>
                                                            </Setter.Value>
                                                        </Setter>
                                                        <Setter Property="Foreground" Value="White"/>
                                                        <Setter Property="BorderThickness" Value="0"/>
                                                        <Setter Property="FontSize" Value="12"/>
                                                        <Setter Property="FontWeight" Value="SemiBold"/>
                                                        <Setter Property="Template">
                                                            <Setter.Value>
                                                                <ControlTemplate TargetType="Button">
                                                                    <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                    </Border>
                                                                    <ControlTemplate.Triggers>
                                                                        <Trigger Property="IsMouseOver" Value="True">
                                                                            <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                                                                        </Trigger>
                                                                    </ControlTemplate.Triggers>
                                                                </ControlTemplate>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </Style>
                                                </Button.Style>
                                            </Button>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </Border>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
