<UserControl x:Class="PcPerformanceManager.Views.PowerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:PcPerformanceManager.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" 
                   Text="Power Management" 
                   FontSize="28" 
                   FontWeight="Bold" 
                   Margin="0,0,0,20"
                   Foreground="#2C3E50"/>

        <!-- Status Bar -->
        <Border Grid.Row="1" 
                Background="#ECF0F1" 
                Padding="15" 
                CornerRadius="5" 
                Margin="0,0,0,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" 
                           Text="{Binding StatusMessage}" 
                           FontSize="14" 
                           VerticalAlignment="Center"
                           Foreground="#34495E"/>
                <Button Grid.Column="1" 
                        Content="Refresh" 
                        Command="{Binding RefreshCommand}"
                        IsEnabled="{Binding IsRefreshing, Converter={StaticResource InverseBooleanConverter}}"
                        Padding="15,8"
                        Margin="10,0,0,0"
                        Cursor="Hand"/>
            </Grid>
        </Border>

        <!-- Power Plans List -->
        <Border Grid.Row="2" 
                Background="White" 
                BorderBrush="#BDC3C7" 
                BorderThickness="1" 
                CornerRadius="8" 
                Padding="20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" 
                           Text="Available Power Plans" 
                           FontSize="18" 
                           FontWeight="SemiBold" 
                           Margin="0,0,0,15"
                           Foreground="#2C3E50"/>

                <ListBox Grid.Row="1" 
                         ItemsSource="{Binding PowerPlans}"
                         SelectedItem="{Binding SelectedPowerPlan}"
                         BorderThickness="0"
                         Background="Transparent">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border Background="White" 
                                    BorderBrush="#BDC3C7" 
                                    BorderThickness="1" 
                                    CornerRadius="5" 
                                    Padding="20" 
                                    Margin="0,0,0,10">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsActive}" Value="True">
                                                <Setter Property="Background" Value="#E8F5E9"/>
                                                <Setter Property="BorderBrush" Value="#27AE60"/>
                                                <Setter Property="BorderThickness" Value="2"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{Binding Name}" FontSize="18" FontWeight="Bold" Foreground="#2C3E50"/>
                                            <TextBlock Text=" (Active)" Foreground="#27AE60" FontSize="14" Margin="5,0,0,0" Visibility="{Binding IsActive, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                        </StackPanel>
                                        <TextBlock Text="{Binding Description}" 
                                                   FontSize="12" 
                                                   Foreground="#7F8C8D"
                                                   TextWrapping="Wrap"
                                                   Margin="0,5,0,0"/>
                                        <TextBlock FontSize="11" Foreground="#95A5A6" Margin="0,5,0,0">
                                            <Run Text="GUID: "/>
                                            <Run Text="{Binding Guid}"/>
                                        </TextBlock>
                                    </StackPanel>
                                    <Button Grid.Column="1" 
                                            Content="Set Active" 
                                            Command="{Binding DataContext.SetActivePowerPlanCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            Padding="20,10"
                                            Background="#27AE60"
                                            Foreground="White"
                                            BorderThickness="0"
                                            Cursor="Hand">
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsActive, Converter={StaticResource InverseBooleanConverter}}" Value="True">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                    </Button>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <Border Grid.Row="2" 
                        Background="#FFF3CD" 
                        Padding="15" 
                        CornerRadius="5" 
                        Margin="0,15,0,0"
                        Visibility="{Binding ActivePowerPlan, Converter={StaticResource NullToVisibilityConverter}}">
                    <StackPanel>
                        <TextBlock Text="Active Power Plan" 
                                   FontWeight="Bold" 
                                   FontSize="14"
                                   Foreground="#856404"
                                   Margin="0,0,0,5"/>
                        <TextBlock FontSize="16" Foreground="#856404">
                            <Run Text="{Binding ActivePowerPlan.Name, FallbackValue='None'}"/>
                        </TextBlock>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl>
